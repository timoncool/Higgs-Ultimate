[default]
The studio lights clicked on just before the first reference phrase began, and the cloned voice should stay steady enough for a direct parity check.
This second sentence keeps the request short so we can compare the repeated request path without changing the speaker identity.

[long_short_long]
MOSS TTS should keep the cloned speaker stable even when a longer paragraph is followed by a shorter sentence and then another longer paragraph. This request is intentionally verbose so the Python reference uses its voice clone chunking path, giving us a real long form parity and performance case instead of a toy prompt that hides structural mistakes in the implementation.
This shorter sentence should still sound like the same speaker and should not force wasteful runtime behavior in the middle of the session.
We then return to another longer paragraph so we can confirm that the runtime revisits the larger workload cleanly, that the generated speech stays intelligible, and that the native path does not drift away from the Python reference after handling mixed request sizes in sequence.
